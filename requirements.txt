python-dotenv>=1.0      # load .env (keys, provider settings)
requests>=2.31          # Ollama Cloud provider (src/providers.py)
anthropic>=0.40         # only used when LLM_PROVIDER=anthropic
chromadb>=0.5
sentence-transformers>=3.0
rank-bm25>=0.2.2
pypdf>=4.0
fastapi>=0.110
uvicorn[standard]>=0.29
scipy>=1.12
